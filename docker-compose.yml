services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: looksy
      POSTGRES_USER: looksy
      POSTGRES_PASSWORD: looksy_dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: looksy
      MINIO_ROOT_PASSWORD: looksy_dev_password
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

volumes:
  postgres_data:
  minio_data: